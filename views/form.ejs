<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Insurance Bill Form</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      max-width: 600px;
      margin: auto;
      background: #f5f5f5;
    }
    form {
      background: white;
      padding: 20px;
      border-radius: 10px;
    }
    input, label, button {
      width: 100%;
      display: block;
      margin-bottom: 10px;
      font-size: 16px;
    }
    fieldset {
      margin-bottom: 20px;
      padding: 10px;
      border: 1px solid #ccc;
    }
    legend {
      font-weight: bold;
    }
    button {
      background-color: #007bff;
      color: white;
      padding: 10px;
      border: none;
      border-radius: 5px;
    }
    @media (max-width: 500px) {
      input, button {
        font-size: 14px;
      }
    }
  </style>
</head>
<body>

  <h2>Insurance Bill Form</h2>

  <form method="POST" action="/generate">

    <label>PDF Heading: </label>
<select name="heading" required>
  <option value="quotation">INSURANCE RENEWAL QOTATION</option>
  <option value="bill">INSURANCE BILL</option>
</select><br><br>

    <label>Trust/Client Name:</label>
    <input name="name" required />

    <label>Date:</label>
    <input name="date" type="date" required />

    <% for (let i = 0; i < count; i++) { %>
      <fieldset>
        <legend>Vehicle <%= i + 1 %></legend>
        <label>Reg No:</label>
        <input name="vehicles[<%= i %>][reg]" required />

        <label>Seat Capacity:</label>
        <input name="vehicles[<%= i %>][seat]" required />

        <label>Insurance Type:</label>
        <input name="vehicles[<%= i %>][ins]" value="package" required />

        <label>Premium:</label>
        <input name="vehicles[<%= i %>][premium]" type="number" required />

        <label>Discount:</label>
        <input name="vehicles[<%= i %>][discount]" type="number" required />

        <label>Payable:</label>
        <input name="vehicles[<%= i %>][payable]" type="number" required />
      </fieldset>
    <% } %>

    <button type="submit">Generate PDF</button>
  </form>

</body>
</html>
